mixin reg()
    .form
        .form__header 
            h1 Зарегистрируйтесь и погрузитесь в музыку
        div
            form(action="/register", method="post")
                div
                    label.form__labels-text(for="email") 
                        h4 Почта
                    input.form__labels(type="email", id="email", name="email" required, placeholder='Email')
                div
                    label.form__labels-text(for="username") 
                        h4 Логин
                    input.form__labels(type="username", id="username", name="username" required, placeholder='Логин')
                div
                    label.form__labels-text(for="password") 
                        h4 Пароль
                    input.form__labels(type="password", id="password", name="password" required, placeholder='Пароль')
                    button.form__button(type="submit")
                        h5 Зарегистрироваться
            div.form__registration-text
                p Уже есть аккаунт?
                a#showLogin(href='#') Авторизация


