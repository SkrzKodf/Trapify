mixin reg()
    form.reg_form(action="/register", method="post")
        div.reg_text 
            h1 Зарегистрируйтесь и погрузитесь в музыку
        div.reg_block_input
            div.reg_input
                label(for="email") Email
                input(type="email", id="email", name="email" required, placeholder = "Email")
            div.reg_input
                label(for="username") Логин
                input(type="text", id="username", name="username" required, placeholder = "Логин")
            div.reg_input
                label(for="password") Пароль
                input(type="password", id="password", name="password" required, placeholder = "Пароль")
        button.reg_but(type="submit") Регистрация
        div.dop_text
            p Есть аккаунт?
            a(href='#') Войти